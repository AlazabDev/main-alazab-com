{
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:frappe": "tsc && vite build --config vite.config.frappe.ts",
    "preview": "vite preview",
    "frappe:build": "./build_for_frappe.sh",
    "frappe:install": "bench get-app azab_shop_revive .",
    "frappe:setup": "bench install-app azab_shop_revive"
  }
}